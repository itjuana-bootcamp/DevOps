name: Capstone Workflow

on:
  push:
  pull_request:
  workflow_dispatch:
 
jobs:
  testjob:
            name: Test Job
            runs-on: ubuntu-latest
            steps:
            - name: Step One
              uses: actions/checkout@v3
            - name: Testing Step
              run: |
                pwd
                
  buildjob:
            needs: [testjob]
            name: Build Job
            runs-on: ubuntu-latest
            steps:
            - name: Step One
              uses: actions/checkout@v3
            - name: Testing Step
              run: |
                echo "build"
  
